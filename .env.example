# Database Configuration
# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@host:5432/database

# Server Configuration
PORT=5000
NODE_ENV=development

# Session Secret (generate a random string for production)
# Used for encrypting session data
SESSION_SECRET=your-secret-key-here

# Optional: Supabase Configuration
# Only needed if using admin file upload features
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: Chromium Path
# Used by web scraping scripts (scripts/scrape_iso_links.py)
# If not set, Playwright will auto-detect Chromium on your system
# CHROMIUM_PATH=/path/to/chromium
# NIX_CHROMIUM_PATH=/nix/store/.../bin/chromium
